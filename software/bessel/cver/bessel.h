#define global
#define forward
#define unless(x)  if(!(x))
#define until(x)   while(!(x))

#define bool	   int
#define false	   0
#define true	   1

#define MAXORDER   10

struct complex { double re, im };

