/*  for (i=0; i<ndata; i++)
      { double x = i*xscale;
	if (((i+1)%PATHLIMIT) == 0) printf ("stroke newpath\n");
	printf ("%d -7 moveto %d 0 lineto\n", (int)x, (int)x);
      }
    printf ("stroke newpath\n");        */
